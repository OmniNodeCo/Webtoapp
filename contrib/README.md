# Webtoapp contribution helpers

This directory contains optional examples for CSS injection and platform
experiments. They are not required for normal builds. Prefer adding a focused
fixture and a unit test under `src/` or `app/src/` when a behavior can be
covered automatically.
